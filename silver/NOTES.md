# Silver stage notes

Written directly through the workbench rather than by the agent, to exercise the direct-edit path: the Workspace Manager validates the path stays inside this task's workspace, writes it, and the round is pushed like any other.

Direct edits are deliberately not scope-checked. The human set the restriction; it constrains the agent, not them.
